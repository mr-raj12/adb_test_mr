# What the API actually needs to run and be tested.
#
# The original file pinned ~80 packages (jupyter, matplotlib, pandas, celery,
# uWSGI, djongo and so on) that nothing here imports. Some of them don't even
# install on a current pip (celery==5.0.5), and the scientific ones build from
# source on Python 3.8, which made the image slow and flaky. A grep over
# src/rest shows the only imports are Django, DRF, cors-headers and pymongo,
# so that's all that's left.

# Web framework + REST toolkit
Django==3.0.5
djangorestframework==3.12.2
django-cors-headers==3.6.0

# MongoDB driver (data is persisted in Mongo, not the ORM/SQLite)
pymongo==3.11.2

# Test-only: in-memory Mongo so the suite needs no running database
mongomock==3.23.0
